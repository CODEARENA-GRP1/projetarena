)
  }
    }
      verification_token: null
      is_verified: false,
    $set: {
  {
  {},
db.users.updateMany(
db.users.insertOne({ username: "omar", email: "omar@example.com", password: "12345" })
db.createCollection("users")
use codearena
docker exec -it codearena-mongo mongosh -u omar -p 12345 --authenticationDatabase admin
db.createCollection("users")
use codearena